# ELECTRON_MIRROR: unset so electron-builder downloads (e.g. dmg-builder) from GitHub.
# To use npmmirror for Electron when installing deps, run:
#   ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ npm install
